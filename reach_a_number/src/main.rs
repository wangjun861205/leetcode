use std::collections::{HashMap, HashSet};

struct Solution;

impl Solution {
    pub fn reach_number_with_map(
        target: i32,
        position: i32,
        map: &mut HashMap<i32, i32>,
        set: &mut HashSet<i32>,
    ) -> i32 {
    }

    pub fn reach_number(target: i32) -> i32 {}
}
fn main() {
    println!("Hello, world!");
}
